export interface MovieModel{
  id? : string;// ? reprezinta ca proprietatea id este optionala (poate sa fie sau poate sanu fie adaugata)
  title : string;
  director : string;
  description : string;
  year : string;
}
